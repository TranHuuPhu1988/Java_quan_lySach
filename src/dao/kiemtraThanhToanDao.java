package dao;
/*
 * import java.sql.PreparedStatement;
 * 
 * public class kiemtraThanhToanDao { DungChung dc = new DungChung();
 * 
 * public int ThemThanhToan(String masach,String ngaymua, String ktrathanhtoan)
 * { dc.KetNoi(); String sql =
 * "INSERT INTO hoadon(makh,NgayMua,damua)VALUES(?,?,?)"; PreparedStatement cmd
 * = dc.cn.prepareStatement(sql); cmd.setString(1, masach); return 1; } }
 */